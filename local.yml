---
- hosts: localhost
  connection: local
  become: true

  tasks:
  - name: install packages
    package:
      name:
        - xorg
        - htop
        - neofetch
        - lightdm
        - qtile
        - picom
        - kitty
        - dconf-editor

  - name: Enabling lightdm
    service:
      name: lightdm
      enabled: yes